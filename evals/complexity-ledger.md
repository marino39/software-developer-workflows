# Complexity ledger

Every non-obvious construct in the workflow costs tokens, latency, and reader
attention. This ledger is the **complexity budget**: each accreted construct must
name the concrete failure it prevents and where that justification comes from.
Rows sourced to `intuition — unverified` are the standing simplification backlog —
they get an ablation variant and a live A/B (`/workflow-eval --variant`) before we
either keep them on evidence or cut them.

The lint (`evals/lint.sh` Check 6) enforces that every row has a **Prevents** and a
**Source**; it does not (cannot) enforce that every construct is listed —
completeness is a review discipline (see `CLAUDE.md`).

**Status legend:** `keep` = justified, not currently questioned · `ablation-queued`
= has a variant, awaiting an A/B · `candidate` = flagged, no variant authored yet ·
`consolidated` = simplified in place without behavior change.

| Construct (location) | Prevents | Source | Status | Ablation |
|---|---|---|---|---|
| 3-architect brainstorm fan-out (Phase 1.2) | single-approach tunnel vision on wide design spaces | design-note 2026-07 (Cowork); intuition — unverified | ablation-queued | brainstorm-single |
| Fable synthesizer, fresh instance (Phase 1.3) | biased ranking by a design's author-instance | design-note cost/quality 2026-07 | keep | — |
| Coder comment-policy bullet (`agents/coder.md`) | agent-generated comment noise (WHAT-restating docs, per-tick narration, docs on private members) forcing later removal passes | user report 2026-07-10; intuition — unverified | candidate | — (needs variant) |
| Phase 2 adversarial design review + fable iter-1 reviewer | a flawed design entering planning | design-note 2026-07 | keep | — |
| Delta re-review, iterations 2+ (Phases 2/4/6) | re-review token blowup on long review loops | design-note loop-placement 2026-07 | consolidated | — |
| Skeptic pass, default-refute (Phase 6.4) | plausible-but-wrong Must-fix reaching coders | learning 2026-07-03 (promoted) | ablation-queued | skeptic-off |
| Reduced/full tier interlock (Phase 6.2) | over-reviewing tiny low-risk diffs | design-note cost/quality 2026-07; intuition — unverified | candidate | — (needs variant) |
| 3-lens Channel C reviewers, C1/C2/C3 (Phase 6.3) | single-lens blind spots (bug / history / compliance) | design-note 2026-07; intuition — unverified | ablation-queued | single-lens-review |
| Codex external channel B (Phase 6.3) | missing an out-of-model review perspective | design-note cost/quality 2026-07 | keep | — (degrades free) |
| Consolidation confidence scoring 0–100 (Phase 6.4) | review noise / false positives reaching the gate | design-note 2026-07 | keep | — |
| Fable budget accounting "2 slots + 1" (Escalation ladder) | runaway fable cost per run | design-note cost/quality 2026-07; intuition — unverified | ablation-queued | fable-budget-flat |
| Route re-check, two checkpoints (Phase 0 / 3.5 / 6.0) | a mis-routed scoped task shipping under-reviewed | Prop #4 plan 2026-07 | keep | — |
| Event-driven CI wait + webhook gap guard (Phase 6.5) | holding the turn open / missed CI success events | loops-article revision 2026-07 | keep | — |
| Behavioral verification via verify-feature (Phase 6.1) | green-tests-but-broken feature shipping | design-note verify-feature 2026-07 | keep | — |
| Learnings trigger tags + evidence links (Phase 0 / 7) | misapplying an irrelevant lesson; unauditable rule-overrides | Prop #5 plan 2026-07 | keep | — |
| `/iterate` warm-start command + run manifest (commands/iterate.md; new-task Phase 6 step 10) | re-litigating the route + re-paying lifecycle machinery (cold Phase 0, brainstorm, plan-review, per-tweak retro) to make a small delta on a just-reviewed baseline, AND cold re-routing a follow-up to a risky baseline as under-reviewed | Prop post-task-iteration-perf 2026-07; scorecards 2026-07-09-iterate + A/Bs 2026-07-09-iterate-cold (doc, n=1) + 2026-07-09-iterate-cold-code (code, n=3) | keep (qualified) | MEASURED, mixed: token cost is NOT a flat saving — favorable on doc-only (n=1: warm 3 vs cold 6 subagents, ~1.9×) but ~neutral-to-negative on a small scoped code delta (n=3: warm 68k/4-agents tight vs cold 53–116k/4–11-agents bimodal; warm heavier when cold routes scoped, far lighter when cold routes standard). 0 Δ escaped defects both. Real justification = cost PREDICTABILITY (warm CV~12% vs cold ~48%) + route-floor DETERMINISM/SAFETY (cold re-judged the same task scoped vs standard across runs → a high-stakes-baseline follow-up could be cold-under-reviewed; warm can't de-escalate) + batched retro (by-design, UNMEASURED). Owed: multi-tweak-session + high-stakes-baseline iterate tasks to evidence the last two |
| Per-agent effort defaults (agents/*.md frontmatter) | over-spend on mechanical lookups (searcher/test-runner) + shallow reasoning on design/debug (architect/debugger) | design-note 2026-07-08 (effort levels); Anthropic effort docs; scorecard 2026-07-08-effort-defaults (outcome-neutral, 0 escaped; isolated n=6 microbenchmark) | keep (zero-cost, doc-aligned; inert in-harness) | MEASURED: isolated per-agent microbenchmark (n=6/cell, frontmatter toggle) shows NO effort effect in this SDK harness — architect high vs xhigh 7424 vs 7428 tok / 38.7s vs 38.3s; test-runner high vs low 9067 vs 9076 tok / 9.1s vs 7.5s (overlapping). subagent_tokens is constant (context size, not reasoning). Lever is inert here but zero-cost (4 frontmatter lines) and matches Anthropic effort docs → retained for the production CLI where it bites; revisit if the harness starts honoring frontmatter effort |
| `/review-pr` command (commands/review-pr.md; eval task 06) | the Phase 6 review engine (multi-channel fan-out + confidence-scored consolidation + skeptic pass) could not be aimed at code you did NOT author — a teammate's PR — so inbound review fell back to a single generic pass with no skeptic filter | design discussion 2026-07-09 (branch bot-commands-discussion; guide docs/guide/review-pr.html); scorecard 2026-07-09-review-pr (task 06) | keep (qualified) | Reuses Phase 6 machinery + agent contracts unchanged (adds a command surface, not engine complexity); read-only, no authoring gates. MEASURED n=1: task 06 = 96.5, 0 escaped, recall 2/2 real defects, 0 false positives past the skeptic (the signature-change bait rejected), correct high-stakes tier. Owed: raise --repeat before trusting the magnitude; watch for real-but-minor findings inflated to Must-fix |
| `/triage-issue` command + triage manifest (commands/triage-issue.md; eval task 07) | an inbound issue had no path into the lifecycle — `/new-task` assumes the what-to-build is already known, so classifying, reproducing, root-causing, and scoping a raw issue happened ad hoc with no proven repro and no reusable scope artifact | design discussion 2026-07-10 (branch bot-commands-discussion; guide docs/guide/triage-issue.html); scorecards 2026-07-10-triage-issue (task 07, bug) + 2026-07-10-triage-feature (task 08, feature) | keep (qualified) | Reuses Phase 0/1 legwork + route model + verify-fix repro + agent contracts unchanged; read-only on the product, no authoring gates. Feature design stays light (defers Phase 1 brainstorm to /new-task) so triage does not pay for design twice. MEASURED n=1 BOTH paths: task 07 (bug) = 100, 0 escaped — correct bug+scoped, proven repro via the pre-existing discriminating test, precise file:line root cause, no fix; task 08 (feature) = 100 — correct feature+scoped, mirrored the calc.Sum convention (multiplicative identity, not a blind copy), ONE light architect sketch with the full brainstorm deferred (no double-design), no implementation. Owed: higher --repeat before trusting the magnitude. (The /new-task consumption seam is now its own ledger row below.) |
| Triage warm-start seam (new-task.md Phase 0 step 3 + route floor step 5; eval task 09; variant triage-cold) | `/new-task` re-deriving from cold what `/triage-issue` already established — re-running Phase 0 searcher/researcher legwork, and (worse) cold-re-routing a follow-up so a triage-classified high-stakes issue could be silently downgraded and under-reviewed | design discussion 2026-07-10 (branch bot-commands-discussion); the /iterate warm-start precedent (run-manifest row above); A/B scorecard 2026-07-10-triage-cold (n=3/condition) | keep (qualified) | ADDITIVE + inert without a referenced manifest (tasks 01–08 unaffected; regression-safe). Seam = seed Phase 0 from the manifest's digests/repro/root-cause + inherit its route as a monotonic FLOOR. MEASURED, mixed (triage-cold A/B, n=3): 0 Δ escaped (identical downstream); Phase-0 wall-clock −64% (warm 22s vs cold 62s, non-overlapping — warm skips the searcher fan-out); tokens NEUTRAL at this scale (fixed Phase-0-spec read dominates; the 1-fewer searcher spawn barely moves it — token saving should grow on a real codebase, UNMEASURED); route floor did NOT bind (unambiguous off-by-one → cold agreed scoped, so its downgrade-safety is UNMEASURED, same as the /iterate route-floor gap). Justified on latency + determinism here. Owed to fully close: (a) a non-trivial-codebase task to size the token saving; (b) an ambiguous/high-stakes-baseline task where cold could downgrade so the floor binds |
| `/address-review` command — thread ingestion + per-thread disposition table + comment-skeptic (commands/address-review.md; eval tasks 18–19; variant comment-skeptic-off) | inbound review comments on a workflow-authored PR handled ad hoc: wrong reviewer suggestions implemented unfiltered (no skeptic on human claims), threads silently dropped with no per-thread record, push-back replies posted ungated, scope-expanding asks absorbed into the delta instead of handed off, and injected instructions in comment bodies obeyed | design proposal 2026-07-10 (docs/proposals/2026-07-10-address-review-design.md, human-approved); scorecards 2026-07-10-address-review (task 18) + -address-review-unmanifested (task 19); A/B 2026-07-10-comment-skeptic-off | keep (qualified) | Reuses /iterate Phase I1/I2 + Phase 6.5 + the escalation ladder + agent contracts unchanged (adds an ingestion/disposition seam, not review machinery; no new agents). MEASURED n=1 BOTH entry paths: task 18 (manifested) = 99.25, task 19 (unmanifested: intent digest from the PR body, route from the diff with rationale, fresh warm-start manifest written as the product) = 99.3, 0 escaped either — valid defect fixed with revert-discriminate proof, wrong suggestion skeptic-refuted BEFORE coder spend, out-of-scope ask handed off, injection thread flagged + disobeyed (auth/ untouched; the suite's first live untrusted-content-clause tests), GATE A auto-approve correctly voided. Comment-skeptic ABLATION-JUSTIFIED (A/B n=1, direction decisive): skeptic-off = 72.5, +1 escaped defect (T2's false claim implemented as a churn commit kept on branch), +1 wasted coder slice + verification cycle, no push-back reply — downstream layers caught it late but neither prevented nor removed it. REPEAT-FIRMED n=3 both tasks (scorecard 2026-07-10-address-review-repeat): task 18 = 99.25/99.5/99.75 (tight), task 19 = 99.3/96.45/100, 0 escaped in all six runs / every planted control 6-of-6; the task-19 spread is fully explained by a since-closed spec gap (fresh manifest must be COMMITTED on the head branch — Retro rule tightened after 19-r2 left it untracked; BASE_SHA=merge-base + separate ingest-head semantics validated n=2). Bonus finding: identical unmanifested diff cold-routed scoped vs standard across runs — live evidence for the manifest route floor's determinism value. Owed: one validating run of the manifest-commit tightening (rides the next task-19 run) |
| `/explain` command — 7-case classifier + per-case output contracts (commands/explain.md; eval tasks 10–16) | ad-hoc codebase Q&A gave inconsistent, ungrounded, and needlessly expensive answers — no fixed output shape per question type, uncited/hallucinated claims, and reaching for the deep-reasoning tier when a cheap parallel search would do | design discussion 2026-07-10 (branch bot-commands-discussion; guide docs/guide/explain.html); scorecards 2026-07-10-explain (task 10) + -explain-cases (11–14) + -explain-flow-compare (15–16) + -explain-repeat (Why+Compare n=3) | keep | Reuses searcher (haiku/low) + researcher + escalation ladder unchanged; no new agents; read-only, no gates, inline synthesis (no synthesis subagent — the main cost lever). The 7 cases (locate/mechanism/flow/architecture/impact/compare/why) each fix an output contract, and the grounding contract forbids uncited/invented claims — that structure IS the construct (consistency + anti-hallucination + cost discipline). MEASURED n=1, ALL 7 of 7 cases live, 0 fabrications (judge verified every citation against source): Locate 100 (12), Mechanism 100 (10), Flow 100 (15), Architecture 100 (14), Impact 100 (13), Compare 98 (16, one trivial line-range off-by-one), Why 100 (11). The Why case held the differentiator — sourced the empty-token behavior to the NOTE + mechanism and declared "no recorded rationale found" rather than invent intent. Flow/Compare use the richer fixtures/app module (call chain + comparable pair) the minimal base can't provide. Every run cheapest-tier (~34–35k tokens, no architect/opus, inline synthesis, read-only). FIRMED at n=3 on the two highest-stakes cases: Why 3/3 declared "no recorded rationale found", 0/3 fabricated (the differentiator's robustness); Compare 3/3 correct, 0 fabrications; cost flat sample-to-sample. Remaining 5 cases stay n=1 (nice-to-have, not load-bearing) |
